pipeline {
  agent { label 'ubuntu-slave' }

  stages {
    stage('Test in Docker') {
      agent {
        dockerContainer {
          image 'node:16-alpine'
        }
      }
      steps {
        sh 'node --version'
      }
    }
  }
}
