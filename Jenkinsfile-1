pipeline {
  agent {
    dockerContainer {
      image 'node:16-alpine'
      label 'ubuntu-slave'
    }
  }

  stages {
    stage('Test') {
      steps {
        sh 'node --version'
      }
    }
  }
}
